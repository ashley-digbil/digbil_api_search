extend layout

block content

	h1 #{title}
	h2 User's last connection data~~~
	p 
	form(id='myForm')
		label(for='input') User Email : 
		input(type='text', name='mail', id='Mail')
		input(type='submit', value='submit', id='Submit')
		p
		textarea(name='ans' ,id='Ans', readonly)

	script(type='text/javascript')
		|$(document).ready(function(){
		|	$('#myForm').submit(function(){
		|		var Mail = $('#Mail').val();
		|		$(this).ajaxSubmit({
		|			type: 'post',
		|			dataType: 'json',
		|			url: '/users',
		|			data: {name: Mail},
		|			success: function(data){
		|				$('#Ans').html(data.message);
		|			},
		|			error: function(e){ 
		|				$('#Ans').html(e.responseText);
		|			}
		|		});
		|		$(this).resetForm();
		|		return false;
		|	})
		|});

