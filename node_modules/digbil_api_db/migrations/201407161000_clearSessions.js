'use strict';
var Promise = require('bluebird');

// Clear all sessions and tokens from the database
module.exports.run = function run(db, logger) {
    var Session = db.get_collection('session');

    return Promise.cast(Session.remove({}).exec());

}
