/**
 * @author Renaud Sauvain		
 */
/**
 * Module dependencies.
 */

/**
 * Exports schema
 */
module.exports.load = function(mg) {
	var Schema = mg.Schema;
	var lock = new Schema({
		'resource' : {
			type : Schema.Types.ObjectId,
			index : true
		},
		'lock' : [ Date ]
	});
	return lock;
};
