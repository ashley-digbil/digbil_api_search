'use strict';

module.exports.load = function(mongoose) {
    var Schema = mongoose.Schema;
    return new Schema({
        name: String,
        date: {
            type: Date,
            default: Date.now,
            index: true
        }
    });
}
